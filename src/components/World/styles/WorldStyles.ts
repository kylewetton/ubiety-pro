import styled from 'styled-components';
import { WorldProps } from '../types';
import {colors, radius, spacing} from '../../../theme';

export const WorldDiv = styled.div<WorldProps>`
    height: 100%;
    width: 100%;
`