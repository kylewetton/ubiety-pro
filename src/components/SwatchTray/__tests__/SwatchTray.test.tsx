import { mount, shallow } from 'enzyme';
import React from 'react';

import SwatchTray from '../SwatchTray';

describe('SwatchTray', () => {
  it('SwatchTray needs some tests', () => {
    const COMPONENT = mount(<SwatchTray />);
    expect(1).toEqual(2); // Fails as a reminder
  });
});
