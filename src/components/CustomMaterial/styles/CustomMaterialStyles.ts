import styled from 'styled-components';
import { CustomMaterialProps } from '../types';
import {colors, radius, spacing} from '../../../theme';

export const CustomMaterialDiv = styled.div<CustomMaterialProps>`
    
`