import styled from 'styled-components';
import { CameraControlsProps } from '../types';
import {colors, radius, spacing} from '../../../theme';

export const CameraControlsDiv = styled.div<CameraControlsProps>`
    
`