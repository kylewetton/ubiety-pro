import { mount, shallow } from 'enzyme';
import React from 'react';

import Selector from '../Selector';

describe('Selector', () => {
  it('Selector needs some tests', () => {
    const COMPONENT = mount(<Selector />);
    expect(1).toEqual(2); // Fails as a reminder
  });
});
