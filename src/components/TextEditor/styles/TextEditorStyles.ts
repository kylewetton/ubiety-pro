import styled from 'styled-components';
import { TextEditorProps } from '../types';
import {colors, radius, spacing} from '../../../theme';

export const TextEditorDiv = styled.div<TextEditorProps>`
    
`